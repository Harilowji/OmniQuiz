services:
  omniquiz:
    build: .
    container_name: omniquiz_fullstack
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - NODE_ENV=production
    volumes:
      - ./data:/app/data
    restart: unless-stopped
